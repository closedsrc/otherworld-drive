<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Photos & videos: granular media permissions (API 33+); READ_EXTERNAL_STORAGE covers older. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Foreground service keeps the "backup now" upload alive when the user leaves the app. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".DfcApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.DfcMobile">

        <!--
          No android:configChanges. The old manifest suppressed
          orientation/screenSize, so rotating the phone skipped the activity
          rebuild entirely and the app kept the layout it was laid out with:

          - turning to landscape left a portrait column stretched sideways
          - the window-class calculation (3/4/6 grid columns, rail vs bar) ran
            once at startup and never again, so a rotation never re-picked it
          - the stale window is what made automated dumps report the wrong
            screen dimensions for the rest of the session

          Compose plus SavedStateHandle already survives the rebuild:
          rememberSaveable holds the tab, sort and layout, and the typed route
          restores the destination. Letting the activity restart is the correct
          behaviour and costs nothing.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".backup.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- WorkManager's foreground service: we call setForeground with the
             dataSync type, so the manifest must declare it too. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- DfcApp implements Configuration.Provider (on-demand init), so the
             androidx.startup WorkManagerInitializer must not also run. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>
