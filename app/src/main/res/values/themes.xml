<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns every colour in the running app. This only covers the
         window before the first frame, so the launch flash matches the theme
         the device is actually in rather than a hardcoded one. -->
    <style name="Theme.DfcMobile" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#FFFFFF</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>
